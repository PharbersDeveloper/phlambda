import defaultPalette from "./property/palette"
import defaultPadding from "./property/padding"
import defaultPieLabel from "../label/pielabel"
import {Theme} from "./theme"

export default new Theme(
	defaultPalette,
	defaultPadding,
	[],
	defaultPieLabel
)
